export default function Watch (props) {
    return (
        <div>

            <div className={`watch-container ${props.darkMode ? "dark" : ""}`}>
                <p>Test</p>

            </div>

        </div>
    )
}
